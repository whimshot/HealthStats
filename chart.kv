#:kivy 1.10.0
#:import MatPlotLib kivy.garden.matplotlib

<Chart>

<BMIChart>

<WeightChart>

<BPChart>

<SmallCharts>

# <SmootBPChart>

<Chartsel>
    loop: True
    direction: 'top'
    wc: wc
    bp: bp
    sc: sc
    # sbp: sbp
    WeightChart:
        id: wc
    BPChart:
        id: bp
    SmallCharts:
        id: sc
    # SmootBPChart
    #     id: sbp
